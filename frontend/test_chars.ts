import { fetchGraphQL } from './lib/graphql'; async function main() { try { const res = await fetchGraphQL(query { characters { id nameEn } }, undefined, 0, undefined); const chars = res.characters.map((c: any) =; console.log('Total characters:', chars.length); console.log('Includes aino:', chars.includes('aino')); console.log('Includes albedo:', chars.includes('albedo')); console.log('Includes amber:', chars.includes('amber')); } catch (e) { console.error(e.message); } } main();  
